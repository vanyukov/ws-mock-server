{
  "name": "websocket-server-node",
  "version": "1.0.0",
  "description": "сервер для тестирования ws подписок",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --env-file=.env src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kirill Vanyukov",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  },
  "optionalDependencies": {
    "utf-8-validate": "^6.0.5"
  },
  "type": "module",
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
